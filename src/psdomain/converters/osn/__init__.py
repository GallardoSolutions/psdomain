"""
Order Shipment Notification (OSN) service converters.

Usage:
    from psdomain.converters.osn import v100
    proto = v100.to_proto(pydantic_response)
    pydantic = v100.from_proto(proto_response)
"""
